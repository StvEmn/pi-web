# 03: 侧栏全项目分组树

**What to build:** 侧栏去掉项目下拉，改为展示所有项目的分组树：按 workspace key 归并（主仓+worktree 同组）、按最近活动排序、组内保留现有会话树（fork 嵌套、运行绿点、重命名/删除等全部既有操作）；每个分组头有「+」按钮，点击在该项目下开一个草稿 tab。侧栏下部的文件浏览器与 worktree 切换器保留，且始终跟随当前激活 tab 所属项目切换。用户从此找任何项目的会话都不需要先「切项目」。

**Blocked by:** 01, 02（「+」复用草稿 tab 模型）

**Status:** ready-for-agent

- [ ] 项目下拉移除；全项目分组树渲染（workspace key 归并、最近活动排序、组内既有会话树操作全保留）
- [ ] 分组头「+」在该项目下开草稿 tab（cwd 正确传递）
- [ ] FileExplorer/WorktreeSwitcher 跟随激活 tab 的项目切换（含跨项目切 tab 时即时跟随）
- [ ] e2e 双项目 fixture：分组树可见两组、组内点会话开 tab、组头「+」开草稿、切 tab 时文件树根切换
- [ ] 会话列表虚拟滚动在多项目全量列表下仍可用（沿用现有虚拟化）
